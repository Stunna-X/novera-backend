[pytest]
addopts = -ra --strict-markers
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests

markers =
    unit: fast tests that do not require a database
    integration: tests that require PostgreSQL and migrated tables
